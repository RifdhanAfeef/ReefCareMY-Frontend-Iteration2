# Copy this file to .env.local to override the deployed backend below
# (e.g. to point at a local FastAPI instance during backend development).
NEXT_PUBLIC_API_BASE_URL=https://reefcare-backend.vercel.app
